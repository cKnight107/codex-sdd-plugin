interface:
  display_name: "Spec Fix"
  short_description: "按 findings 做增量修正，并协同 debug 与 TDD 技能同步 spec tasks log"
  default_prompt: "使用 $spec-fix 根据 review 结果对当前实现做增量修正；先复现并定位，再修复、补证明，同步 spec.md、tasks.md、log.md。"
